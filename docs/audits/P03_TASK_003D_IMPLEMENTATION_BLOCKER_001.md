# TASK-P03-003D — Implementation blocker 001

- **Release artifact:** `3f2f2ca1e15555e0db3593870c92403f0be2476d`; immutable contract `docs/tasks/TASK_CONTRACT_P03_003D.md`, base SHA `583e700eb125a08cc6bd7d63b6b27a6f3d4cc527`.
- **Partial implementation:** `908bf36bb57f343f86e900c9225d3f8372693ddf` on `codex/p03-003d`. Only `internal/store/recovery_transactions.go` and its test changed. Restore crash Tx C and provisioning startup classification have Store transaction/audit and rollback/replay tests. This is **not** AC-3D-01..12 completion or implementation approval.
- **BLOCKER-3D-001 — post-send outage audit event:** Released contract AC-3D-09 and matrix §3 require exact CAS to `RECOVERY_PENDING` plus outage audit for `DISPATCHED/SEND_CONFIRMED` and `RUNNING`. ADR-016 Canonical Token Registry §4 item 8 lists audit event types, but none denotes post-send AO-unavailable observation or this hold transition. `PRE_SEND_ADMISSIBILITY_REJECTED` and addendum `PRE_SEND_STATUS_RECOVERED` apply to pre-send; `STARTUP_RECOVERY_SWEEP_STARTED/COMPLETED` record the sweep, not a per-attempt outage mutation. `RECOVERY_PENDING` is a disposition, not approved `event_type`. No event is invented or relabeled.
- **BLOCKER-3D-002 — restart stop reissue resolution audit event:** ADR-016 D13 §19 requires `STOP_REQUESTED/IN_FLIGHT` with exact alive target to become `STOP_REISSUE_REQUIRES_HUMAN` without `/kill` replay; contract AC-3D-04/11 requires atomic CAS/audit. Accepted clarification `ADR-016-CLARIFICATION-stop-operation-resolved-audit.md` restricts `STOP_OPERATION_RESOLVED` to `STOP_GENERATION_MISMATCH` and `STOP_EFFECT_UNPROVEN_TARGET_ALREADY_TERMINATED`. No approved `event_type` covers the live-target `STOP_REISSUE_REQUIRES_HUMAN` mutation. Generic stop updates cannot supply the missing provenance safely.
- **Decision requested:** External Supervisor should approve exact event type(s), audit details and transaction semantics for the two cases through the change governance path, or explicitly revise the released 3D contract/ADR semantics. Keep accepted ADR and contract unchanged until that decision. No fallback to unrelated audit events or new token in code.
- **Verification of partial work:** `go test -count=1 ./internal/store/...` exit 0; `go test -race -count=1 ./internal/store/...` exit 0; `go test -count=1 ./...` exit 0; diff `base_sha..908bf36` whitelist 2/2 and `git diff --check` exit 0. Recovery verification request cannot run because blocked scanner/poller package is not implemented. No live AO effect or user database migration.
- **State:** `TASK_P03_003D=RELEASED`; `TASK_P03_003D_IMPLEMENTATION=BLOCKED`; `P03_CODE=AUTHORIZED_3D_ONLY` within released scope, dependent code held; `ACTIVE_GATE=TASK_P03_003D_IMPLEMENTATION`. `AUTOMATIC_RESTORE=DISABLED`, verified host principal dependency and `DESIGN_BLOCKER_3D_STARTUP_WIRING=PRESERVED`.