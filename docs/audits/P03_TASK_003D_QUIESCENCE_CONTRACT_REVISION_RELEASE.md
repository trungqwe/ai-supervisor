# TASK-P03-003D revision 2 — quyết định và release

- **Authority:** External Supervisor phê duyệt `EXCLUSIVE HOST QUIESCENCE` để tháo `BLOCKER-3D-003` ở cấp thiết kế. ADR addendum và `CONTRACT-TASK-P03-003D-02` được ghi trong commit governance này; SHA commit được báo riêng, không nhúng tự tham chiếu.
- **Historical artifact:** `CONTRACT-TASK-P03-003D-01` tại `3f2f2ca1e15555e0db3593870c92403f0be2476d` giữ nguyên. Revision 2 supersedes ID đó, `revision_number=2`, code `base_sha=583e700eb125a08cc6bd7d63b6b27a6f3d4cc527`, whitelist 19/19 entry không đổi. AC-3D-01..12 giữ nguyên; AC-3D-13/14 chốt quiescence ownership và deterministic barrier evidence.
- **Validation:** `go run p03_3d_revision_validate_tmp.go` (harness tạm đã xóa) chạy full schema `docs/schemas/task-contract.schema.json` qua `TaskContractValidator.ValidateRaw` với previous contract immutable và catalog `go-test-p03-003d`: `CwdPolicy=worktree_root`, `MaxTimeoutSeconds=300`, parameter object `additionalProperties=false`, required `package`/`flags`, package enum `./internal/recovery/...`, `./internal/store/...`, `./...`, flags array enum duy nhất `["-v","-race"]`. Revision PASS; timeout=301, flag=-exec, package `./internal/ao/...`, cwd=`../escape` đều REJECTED; exit 0. Đây là validation library/catalog, không chứng minh P04 runner hoặc host quiescence.
- **Gate:** `BLOCKER-3D-003=CLOSED_BY_APPROVED_ADDENDUM` ở cấp quyết định; `TASK_P03_003D_IMPLEMENTATION=IN_PROGRESS`, chưa có implementation approval. `HOST_QUIESCENCE_INTEGRATION=OPEN`; `DESIGN_BLOCKER_3D_STARTUP_WIRING=PRESERVED`, verified host principal dependency và `AUTOMATIC_RESTORE=DISABLED`.
