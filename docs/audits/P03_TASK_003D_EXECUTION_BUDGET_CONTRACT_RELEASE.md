# CONTRACT-TASK-P03-003D-03 — release audit

**Authority:** External Supervisor approved canonical reconciliation at `68194f739fc5a514cd1f7850027771a77a866d47` and release from candidate blob `0c15ca55403681f8647e5b2dc242fcf91ec5a521`. Code base SHA remains `583e700eb125a08cc6bd7d63b6b27a6f3d4cc527`; existing implementation branch HEAD before continuation is `00f64d0b26db2749eadd21b7307c98faa6b6280d`.

`docs/tasks/TASK_CONTRACT_P03_003D_REVISION_3.md` supersedes immutable revision 2. Candidate, released revisions 1/2, ADRs and audit history are unchanged. Candidate-to-artifact differences are **release metadata only**: title/status/dependency and explanatory prose change from pending to released; two JSON status constraints now state released authorization instead of pending release. Requirements, `base_sha`, exact `allowed_scope`/`forbidden_scope`, AC-3D-01..19, verification requests and technical semantics are unchanged. No self-referential release SHA is embedded in the artifact.

**Release gate:** `TASK_P03_003D_REVISION_3=RELEASED`, `TASK_P03_003D_IMPLEMENTATION=REVISION_REQUIRED` pending re-audit, `P03_CODE=AUTHORIZED_3D_ONLY`, `ACTIVE_GATE=TASK_P03_003D_REMEDIATION`. `3D-R1-001..005` remain open until external implementation audit. `AUTOMATIC_RESTORE=DISABLED`; verified host principal, `HOST_QUIESCENCE_INTEGRATION=OPEN`, `DESIGN_BLOCKER_3D_STARTUP_WIRING=PRESERVED`. Fake test harness does not establish runtime authentication/cross-process exclusivity.

**Validation trước implementation:** `go run p03_exec_release_validate_tmp.go` (harness tạm, xóa sau kiểm tra) gọi `CompileSchema`/`ParseAndValidateRaw` với full `docs/schemas/task-contract.schema.json` và `TaskContractValidator.ValidateRaw` với approved in-memory `go-test-p03-003d` catalog: `CwdPolicy=worktree_root`, timeout tối đa 300, parameter object `additionalProperties=false`, required `package`/`flags`, package enum `./internal/recovery/...`, `./internal/store/...`, `./...`, flags chỉ `["-v","-race"]`. Full schema và semantic PASS; timeout 301, `-exec`, package ngoài enum và cwd `../escape` đều REJECTED; exit 0. So sánh JSON candidate/artifact: chỉ `constraints` và `stop_conditions` thay câu trạng thái release, các trường còn lại giống hệt; exit 0. Validation library/catalog không chứng minh P04 runner.
