# TASK-P03-003D execution budget — quyết định thiết kế

**Authority:** External Supervisor decision trên hồ sơ governance `3babaa2ba573fcdd7d6b34cfb5ea2fd44f1fffe4`. **Verdict:** design approved với ràng buộc EXEC-R1-001..003; không phê duyệt implementation `00f64d0b26db2749eadd21b7307c98faa6b6280d`, không release contract revision 3. Supervisor đã kiểm tra diff hygiene và code Store; chưa tự chạy lại schema validator, nên hồ sơ này không gán PASS validation thay Supervisor.

| Finding | Quyết định ràng buộc | Nơi ghi |
| --- | --- | --- |
| EXEC-R1-001 | Trusted maintenance độc quyền dùng cùng host boundary; normal admission/serve đóng, drain/join và loại trừ Run/caller xung đột. Historical binding và manual stop khác authority. Manual stop chỉ exact current open `RUNNING`; legacy `DISPATCHED` không ép `RUNNING`, thiếu reconciliation hợp lệ giữ fail-closed. Release scope không mở serve, phải Run lại. | ADR execution budget §6.1; docs/14 REC-004; AC-3D-18. |
| EXEC-R1-002 | Tx R tái dùng 3C reservation với stop intent, double quarantine và audit trong một transaction; own-reservation quarantine không tự chặn winner trước effect. Caller bỏ effect giữ `STOP_REQUESTED/IN_FLIGHT`, attempt mở, double quarantine, không ghi resolution audit. Chỉ exclusive recovery/human sau drain/join phân loại; 404/terminated/generation mismatch dùng D11. `RelinquishTimeoutEffect` bị loại. | ADR §§4–5.1; docs/06 §4; AC-3D-17/19. |
| EXEC-R1-003 | Thêm chính xác `internal/store/migrations_v4_test.go` vào whitelist, chỉ sửa assertion migrate-to-current; giữ fixture/schema v4 và rollback lịch sử. | Candidate allowed_scope; AC-3D-15; docs/21 coverage. |

**Giới hạn:** accepted ADR-016, các clarification, audit lịch sử và contract revision 2 giữ nguyên. Addendum chỉ supersede origin/binding execution budget, admission timeout stop và legacy maintenance được liệt kê. `3D-R1-005=OPEN` đến implementation audit; `3D-R1-001..004` không được tự đóng bằng quyết định tài liệu. `HOST_QUIESCENCE_INTEGRATION=OPEN`, verified host principal và `DESIGN_BLOCKER_3D_STARTUP_WIRING=PRESERVED`; `AUTOMATIC_RESTORE=DISABLED`. Candidate revision 3 `NOT_RELEASED`, code gate vẫn `TASK_P03_003D_REMEDIATION`; không migration/Go/merge trong lượt này.

**Kiểm tra tài liệu trong lượt reconcile:** full schema (`CompileSchema` + `ParseAndValidateRaw`) và `TaskContractValidator.ValidateRaw` với catalog `go-test-p03-003d` đã duyệt cho draft/candidate: PASS, exit 0. JSON draft/candidate đồng nhất. Bốn negative mutation timeout 301, flags `[-v,-exec]`, package `./internal/ao/...`, cwd `../escape`: đều REJECTED, harness exit 0. SQL DDL trong draft ADR và ADR chính thức giống hệt baseline `3babaa2` (SHA-256 `ba53215d3b1a262583c61487006ce7dc3dfc776fb8970263b08bd2980c195b10`), nên không có SQL mới cần probe; đây là so khớp văn bản, **không** là bằng chứng migration behavior. Validation library/catalog không chứng minh P04 host runner hoặc host authentication.
